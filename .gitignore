node_modules/
build/
.env
bun.lockb

# Sentry Config File
.sentryclirc

src/generated
.vrchat_cookies/

# Local upstream clones / reference (not part of the app)
.upstream/

# Local Vitest reporters / probes (CI writes these under predictable paths too)
test-results/
